# Network selection
NETWORK=regtest   # regtest|testnet|mainnet

# Bitcoin Core RPC & ZMQ (edit for your setup)
BITCOIN_RPC_URL=http://127.0.0.1:18443
BITCOIN_RPC_USER=solo
BITCOIN_RPC_PASS=solo123
BITCOIN_ZMQ_BLOCK=tcp://127.0.0.1:28342
BITCOIN_ZMQ_TX=tcp://127.0.0.1:28343

# Listeners (safe default: web bound to localhost)
STRATUM_ADDR=0.0.0.0:3335
WEB_ADDR=127.0.0.1:8080

# Storage
POOL_DB=./data/pool.sqlite

# Mainnet requirement (pool exits if missing)
# PAYOUT_ADDRESS=bc1...

# Optional: require password on mining.authorize
# STRATUM_PASSWORD=change_me

# Logging (RUST_LOG compatible)
LOG_FILTER=info,solo_rs_pool=info
